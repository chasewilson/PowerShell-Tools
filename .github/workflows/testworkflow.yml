name: Testing Stuff

on:
  issues:
    types: [opened]

defaults:
  run:
    shell: pwsh
env:
  PAYLOAD: "${{github.event}}"
jobs:
  test_job:
    name: Test Job
    runs-on: windows-latest
    steps:
      - name: Get Information on the job.
        run: Write-Output $env:PAYLOAD
